import { RotateIconDirective } from './rotate-icon.directive';

describe('RotateIconDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateIconDirective();
    expect(directive).toBeTruthy();
  });
});
